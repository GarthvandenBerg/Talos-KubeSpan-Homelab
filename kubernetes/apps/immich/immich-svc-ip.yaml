apiVersion: v1
kind: Service
metadata:
  name: immich-server-lan
  namespace: immich
  annotations:
    io.cilium/lb-ip-am-ips: "192.168.2.205"
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: immich
    app.kubernetes.io/component: server
  ports:
    - name: http
      port: 2283
      targetPort: 2283
      protocol: TCP
